/// @description Insert description here
// You can write your code in this editor
event_inherited()
inventory.add_item(
    instance_create_layer(x, y, layer, oSwordParent)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oShieldParent)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oSpearParent)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oBoots)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oGloves)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oTshirt)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oPants)
)
inventory.add_item(
    instance_create_layer(x, y, layer, oCap)
)